import { StyleSheet } from 'react-native';
import colors from '../../config/colors';

export const IMAGE_SIZE = 22;

export default StyleSheet.create({
    drawerButtonContainer: {
        marginLeft: 20,
      },
});